<form method="post">
<div class="card shadow-sm">
  <div class="card-header">
    <h3 class="card-title">Tipos de Ativo</h3>
  </div>
  <div class="card-body">
    <div class="row">
      {% for item in assets %}
      <div class="col-md-6 mb-3">
        <label class="w-100">
          <div class="card h-100">
            <div class="card-body d-flex justify-content-between">
              <div>
                <strong>{{ item.label }}</strong><br>
                <small class="text-muted">
                  Aplicar configuração para {{ item.label|lower }}
                </small>
              </div>
              <input type="checkbox"
                     name="asset[{{ item.id }}]"
                     value="1"
                     {% if item.enabled %}checked{% endif %}>
            </div>
          </div>
        </label>
      </div>
      {% endfor %}
    </div>
  </div>
</div>
<div class="row justify-content-center mt-3">
  <div class="col-md-6">
    <div class="card shadow-sm h-100">
      <div class="card-header">
        <h3 class="card-title">Conexão</h3>
      </div>
      <div class="card-body">
        <div class="mb-3">
          <label class="form-label" for="ip">Endereço IP</label>
          <input type="text" class="form-control" id="ip" name="ip"
                 value="{{ ip }}" placeholder="192.168.0.1">
        </div>
        <div class="mb-3">
          <label class="form-label" for="port">Porta</label>
          <input type="number" class="form-control" id="port" name="port"
                 value="{{ port }}" min="1" max="65535" placeholder="8080">
        </div>
      </div>
    </div>
  </div>

  <div class="col-md-6">
    <div class="card shadow-sm h-100">
      <div class="card-header">
        <h3 class="card-title">Modelo ZPL</h3>
      </div>
      <div class="card-body">
        <div class="mb-3">
          <label class="form-label" for="zpl">Código ZPL</label>
          <textarea class="form-control font-monospace" id="zpl" name="zpl"
                    rows="12" placeholder="^XA&#10;...&#10;^XZ">{{ zpl }}</textarea>
                    <small class="text-muted">
                      Marcadores disponíveis: {{ '{{ID}}' }}, {{ '{{NOME}}' }}, {{ '{{SERIAL}}' }}, {{ '{{RESPONSAVEL}}' }}, {{ '{{URL}}' }}
                    </small>
        </div>
      </div>
    </div>
  </div>
</div>
<button type="submit" name="update" class="btn btn-primary mt-3">
Salvar
</button>
<input type="hidden" name="_glpi_csrf_token" value="{{ csrf_token() }}">
</form>